#include <iostream>  // import input and output library that are part of the standard library
#include ""          // use "" to include headers outside the standard lib
using namespace std; // local variable scope something...
/* finish every statement with ;
avoid using global variables to prevent wasting memory
*/

int main() // init a function of the form: int [name](var1, var2, ...) {body content}
{
  float a;
  a = 5;
  cout << "Hi" << endl;

  return 0;
}